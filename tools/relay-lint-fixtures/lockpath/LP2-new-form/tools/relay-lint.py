# LP2 locator target. The fixture carries its own lock-path artifact so the
# relay-root probe resolves `tools/relay-lint.py` from any CWD; the digest in
# the fixture's annotation is not compared in this mode (existence probe).
